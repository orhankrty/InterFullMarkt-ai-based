@{
    ViewData["Title"] = "KayÄ±t Ol";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-5 col-md-7">
            <div class="card shadow-lg border-0 rounded-4 mt-5 overflow-hidden">
                <div class="card-header bg-success text-dark text-center py-4">
                    <h3 class="font-weight-light mb-0"><i class="bi bi-person-plus-fill me-2"></i> Yeni Hesap OluÅŸtur</h3>
                </div>
                <div class="card-body p-5 bg-light">
                    
                    @if (ViewData["Error"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show shadow-sm" role="alert">
                            <i class="bi bi-exclamation-triangle-fill me-2"></i> @ViewData["Error"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    <form asp-controller="Auth" asp-action="Register" method="post">
                        @Html.AntiForgeryToken()
                        
                        <div class="form-floating mb-4">
                            <input type="text" class="form-control rounded-3" id="username" name="username" placeholder="KullanÄ±cÄ± AdÄ±" required autofocus />
                            <label for="username" class="text-muted"><i class="bi bi-person me-1"></i> KullanÄ±cÄ± AdÄ±</label>
                        </div>

                        <div class="form-floating mb-4">
                            <input type="email" class="form-control rounded-3" id="email" name="email" placeholder="name@example.com" required />
                            <label for="email" class="text-muted"><i class="bi bi-envelope me-1"></i> E-posta Adresi</label>
                        </div>
                        
                        <div class="form-floating mb-4">
                            <input type="password" class="form-control rounded-3" id="password" name="password" placeholder="Åifre" required />
                            <label for="password" class="text-muted"><i class="bi bi-key me-1"></i> Åifre</label>
                        </div>
                        
                        <div class="d-grid gap-2 mt-4">
                            <button type="submit" class="btn btn-success btn-lg rounded-pill shadow-sm fw-bold">
                                KayÄ±t Ol <i class="bi bi-check-circle ms-1"></i>
                            </button>
                        </div>
                    </form>
                </div>
                <div class="card-footer text-center py-4 bg-white border-top-0">
                    <div class="small">
                        <span class="text-muted">Zaten hesabÄ±nÄ±z var mÄ±?</span> 
                        <a asp-controller="Auth" asp-action="Login" class="text-decoration-none fw-bold ms-1 text-success">Buradan GiriÅŸ YapÄ±n!</a>
                    </div>
                </div>
            </div>
            
            <div class="text-center mt-4">
                <p class="text-muted small">Not: Ãœyelik iÅŸlemleri ÅŸu an iÃ§in tanÄ±tÄ±m amaÃ§lÄ±dÄ±r.</p>
            </div>
        </div>
    </div>
</div>
